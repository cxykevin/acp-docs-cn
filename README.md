# ACP 文档

ACP 文档使用 [Mintlify](https://mintlify.com/) 构建。
所有文件均为 MDX 格式，如有需要可以承载自定义组件。

## 运行

运行以下命令以在本地启动文档：

```bash
npm run docs
```

## 本地预览更改

要在本地预览更改，请运行以下命令：

```bash
mint dev
```

### 安装 CLI

在本地运行站点之前，您需要安装 Mint 的 CLI：

```bash
npm i -g mint
```

## 部署

每当更改合并到 `main` 分支时，文档站点都会自动更新。
